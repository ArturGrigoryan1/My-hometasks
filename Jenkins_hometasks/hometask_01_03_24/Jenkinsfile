pipeline {
    agent { label 'jenkins_agent' }

    stages {
        stage('Project') {
            steps {
                sh 'ls -ls'
                sh 'python3 --version'
                sh 'python3 get_uniqes.py'
            }
        }
    }
}
